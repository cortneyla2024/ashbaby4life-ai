# Enable pnpm workspace protocol
node-linker=hoisted
shamefully-hoist=true
auto-install-peers=true

# Enable workspace protocol
workspace-concurrency=1

# Ensure workspace dependencies are resolved correctly
public-hoist-pattern[]=*
